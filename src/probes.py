"""Direction/probe training on saved activations. Stub -- fleshing out next.

Per CLAUDE.md: LogisticRegression(max_iter=2000, C=0.1); split indices once
and reuse across layers; orient directions so positive = true/honest.
"""
